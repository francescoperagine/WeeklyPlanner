import { Routes } from '@angular/router';
import { ReflectionListComponent } from './components/reflection-list/reflection-list.component';

export const REFLECTION_ROUTES: Routes = [
  { path: '', component: ReflectionListComponent }
];